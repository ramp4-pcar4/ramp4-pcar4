// any modules without a declaration file can be manually added here to keep typescript happy (though its not necessarily required)

// declare module 'js-sql-parser';
// ^ has been removed, but keeping this line to serve as template for anything else that needs to get added
