import { FixtureInstance } from '../../../api';
export declare class WizardAPI extends FixtureInstance {
    /**
     * Opens or closes the wizard fixture panel
     *
     * @memberof WizardAPI
     * @param open force panel open or closed
     */
    toggleWizard(open?: boolean): void;
}
